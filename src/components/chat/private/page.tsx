// DM Interface
// route -> /conversation/userId1-userId2(conversationId = userId1-userId2)
// 1. (LEFT) LIST OF FRIENDS FOR GIVEN USERS, WITH TITLE AS "Conversations" + (RIGHT) Select a Conversation
// 2. onclick a conversation -> that conversation(/conversation/conversationId) pops up on (RIGHT), (LEFT) remains same

export function DirectMessagesChat(){

    
}

export function DM({chatId} : {chatId: string}){
    
}

export function ConversationsListForGivenUser({userId} : {userId: string}){

}

export function Dashboard(){
    
}