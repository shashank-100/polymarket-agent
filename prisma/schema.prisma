model User {
  id               String    @id @default(cuid())
  walletPublicKey  String    @unique
  username         String?
  createdAt        DateTime  @default(now())
  lastLogin        DateTime?
}